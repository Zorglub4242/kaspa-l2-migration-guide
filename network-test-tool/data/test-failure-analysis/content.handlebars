<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Test Failure Analysis</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; background: linear-gradient(135deg, #fc4a1a 0%, #f7b733 100%); padding: 20px; }
        .container { max-width: 1400px; margin: 0 auto; background: white; border-radius: 10px; padding: 40px; }
        h1 { color: #2c3e50; border-bottom: 3px solid #dc3545; padding-bottom: 10px; margin-bottom: 20px; }
        .failure-summary { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 20px; margin: 30px 0; }
        .failure-card { background: #f8d7da; padding: 20px; border-radius: 8px; text-align: center; border: 2px solid #dc3545; }
        .failure-value { font-size: 32px; font-weight: bold; color: #dc3545; }
        .failure-label { color: #721c24; font-size: 12px; text-transform: uppercase; margin-top: 5px; }
        .error-category { margin: 20px 0; padding: 20px; background: #f8f9fa; border-radius: 8px; }
        .error-list { list-style: none; padding: 0; }
        .error-item { padding: 15px; margin: 10px 0; background: white; border-left: 4px solid #dc3545; border-radius: 5px; }
        .error-header { display: flex; justify-content: space-between; margin-bottom: 10px; }
        .error-test { font-weight: bold; color: #2c3e50; }
        .error-count { background: #dc3545; color: white; padding: 2px 8px; border-radius: 12px; font-size: 12px; }
        .error-message { color: #dc3545; font-family: monospace; font-size: 14px; margin: 10px 0; padding: 10px; background: #f8f9fa; border-radius: 4px; }
        .root-cause { background: #fff3cd; padding: 20px; border-radius: 8px; border-left: 4px solid #ffc107; margin: 20px 0; }
        .fix-suggestion { background: #d1ecf1; padding: 20px; border-radius: 8px; border-left: 4px solid #17a2b8; margin: 20px 0; }
    </style>
</head>
<body>
    <div class="container">
        <h1>❌ Test Failure Analysis</h1>
        <p style="color: #7f8c8d;">Generated: {{formatTime timestamp}} | Analysis Period: {{period}}</p>

        <div class="failure-summary">
            <div class="failure-card">
                <div class="failure-value">{{totalFailures}}</div>
                <div class="failure-label">Total Failures</div>
            </div>
            <div class="failure-card">
                <div class="failure-value">{{uniqueErrors}}</div>
                <div class="failure-label">Unique Errors</div>
            </div>
            <div class="failure-card">
                <div class="failure-value">{{affectedTests}}</div>
                <div class="failure-label">Affected Tests</div>
            </div>
            <div class="failure-card">
                <div class="failure-value">{{failureRate}}%</div>
                <div class="failure-label">Failure Rate</div>
            </div>
        </div>

        <h2>📊 Failures by Category</h2>
        {{#each errorCategories}}
        <div class="error-category">
            <h3>{{category}} ({{count}} failures)</h3>
            <ul class="error-list">
                {{#each errors}}
                <li class="error-item">
                    <div class="error-header">
                        <span class="error-test">{{test_name}}</span>
                        <span class="error-count">{{occurrences}}x</span>
                    </div>
                    <div class="error-message">{{error_message}}</div>
                    <div style="font-size: 12px; color: #6c757d; margin-top: 10px;">
                        Network: {{network}} | Last occurred: {{formatTime last_occurrence}}
                    </div>
                </li>
                {{/each}}
            </ul>
        </div>
        {{/each}}

        <div class="root-cause">
            <h2>🔍 Root Cause Analysis</h2>
            <ul style="margin-top: 15px; padding-left: 20px;">
                {{#each rootCauses}}
                <li>
                    <strong>{{cause}}:</strong> {{description}}
                    <ul style="margin-top: 5px; margin-left: 20px;">
                        {{#each affectedTests}}
                        <li>{{this}}</li>
                        {{/each}}
                    </ul>
                </li>
                {{/each}}
            </ul>
        </div>

        <div class="fix-suggestion">
            <h2>💡 Suggested Fixes</h2>
            <ol style="margin-top: 15px; padding-left: 20px; line-height: 1.8;">
                {{#each suggestions}}
                <li>
                    <strong>{{title}}:</strong> {{description}}
                    {{#if code}}
                    <pre style="margin-top: 10px; padding: 10px; background: #f8f9fa; border-radius: 4px; font-size: 12px;">{{code}}</pre>
                    {{/if}}
                </li>
                {{/each}}
            </ol>
        </div>

        <h2>📈 Failure Trends</h2>
        <ul style="line-height: 2; padding-left: 20px;">
            <li>Most failures occur during: {{peakFailureTime}}</li>
            <li>Network with most failures: {{worstNetwork}}</li>
            <li>Most problematic test type: {{worstTestType}}</li>
            <li>Average time to resolution: {{avgResolutionTime}}</li>
        </ul>

        <h2>🎯 Priority Actions</h2>
        <ol style="line-height: 2; padding-left: 20px;">
            <li>Fix gas estimation errors affecting {{gasErrorPercentage}}% of failures</li>
            <li>Improve network timeout handling for better resilience</li>
            <li>Add retry logic for transient network errors</li>
            <li>Update test assertions to handle edge cases</li>
        </ol>
    </div>
</body>
</html>